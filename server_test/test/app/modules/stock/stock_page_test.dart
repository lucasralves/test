import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:servertest/app/modules/stock/stock_page.dart';

void main() {
  testWidgets('StockPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StockPage(title: 'Stock')));
    //  final titleFinder = find.text('Stock');
    //  expect(titleFinder, findsOneWidget);
  });
}
